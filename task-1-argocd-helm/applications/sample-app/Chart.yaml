apiVersion: v2
name: sample-app
description: Sample application for GitOps demonstration
type: application
version: 1.0.0
appVersion: "1.0"

# Metadata for helm repository
keywords:
  - sample
  - gitops
  - argocd

maintainers:
  - name: Platform Team
    email: platform@example.com

# Shared templates come from the common library chart (vendored into charts/).
dependencies:
  - name: common
    version: 1.0.0
    repository: file://../../charts/common
