apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-patch-and-transform
spec:
  # Crossplane v2 removed native (spec.resources) patch-and-transform.
  # Compositions now run a pipeline of functions. This function provides
  # the same patch-and-transform behavior the Composition relies on.
  package: xpkg.crossplane.io/crossplane-contrib/function-patch-and-transform:v0.8.2
